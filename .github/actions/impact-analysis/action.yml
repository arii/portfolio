name: 'BoomTick Impact Analysis'
description: 'Runs impact analysis and AI code reviews in the BoomTick container'
inputs:
  github_token:
    description: 'GitHub token for PR comments and API calls'
    required: true
  gemini_api_key:
    description: 'Gemini API key for Gemini reviewer'
    required: false
  openai_api_key:
    description: 'OpenAI API key for OpenAI/GitHub-models reviewer path'
    required: false
  jules_api_key:
    description: 'Jules API key for agent execution'
    required: false
  pr_number:
    description: 'Optional PR number override (for workflow_call/workflow_dispatch)'
    required: false
runs:
  using: 'composite'
  steps:
    - name: Run Impact Analysis and Reviewers in Container
      shell: bash
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        GEMINI_API_KEY: ${{ inputs.gemini_api_key }}
        OPENAI_API_KEY: ${{ inputs.openai_api_key }}
        JULES_API_KEY: ${{ inputs.jules_api_key }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        INPUT_PR_NUMBER: ${{ inputs.pr_number }}
        EVENT_PR_NUMBER: ${{ github.event.pull_request.number }}
      run: |
        PR_NUMBER="${INPUT_PR_NUMBER:-$EVENT_PR_NUMBER}"
        if [ -z "$PR_NUMBER" ]; then
          echo "::warning::PR number not available; skipping PR-scoped AI review steps."
        fi

        docker run --rm \
          --entrypoint /workspace/node_modules/.bin/tsx \
          -e GITHUB_TOKEN="${GITHUB_TOKEN}" \
          -e GEMINI_API_KEY="${GEMINI_API_KEY}" \
          -e OPENAI_API_KEY="${OPENAI_API_KEY}" \
          -e JULES_API_KEY="${JULES_API_KEY}" \
          -e PR_NUMBER="${PR_NUMBER}" \
          -e GITHUB_REPOSITORY="${GITHUB_REPOSITORY}" \
          -v "${{ github.workspace }}":/github/workspace \
          -w /github/workspace \
          ghcr.io/arii/boomtick:latest \
          /workspace/scripts/impact-analysis.ts

        if [ -n "${GEMINI_API_KEY}" ] && [ -n "${PR_NUMBER}" ]; then
          docker run --rm \
            --entrypoint /workspace/node_modules/.bin/tsx \
            -e GITHUB_TOKEN="${GITHUB_TOKEN}" \
            -e GEMINI_API_KEY="${GEMINI_API_KEY}" \
            -e JULES_API_KEY="${JULES_API_KEY}" \
            -e PR_NUMBER="${PR_NUMBER}" \
            -e GITHUB_REPOSITORY="${GITHUB_REPOSITORY}" \
            -v "${{ github.workspace }}":/github/workspace \
            -w /github/workspace \
            ghcr.io/arii/boomtick:latest \
            /workspace/scripts/impact-ai-code-review.ts gemini
        fi

        if [ -n "${OPENAI_API_KEY}" ] && [ -n "${PR_NUMBER}" ]; then
          docker run --rm \
            --entrypoint /workspace/node_modules/.bin/tsx \
            -e GITHUB_TOKEN="${GITHUB_TOKEN}" \
            -e OPENAI_API_KEY="${OPENAI_API_KEY}" \
            -e JULES_API_KEY="${JULES_API_KEY}" \
            -e PR_NUMBER="${PR_NUMBER}" \
            -e GITHUB_REPOSITORY="${GITHUB_REPOSITORY}" \
            -v "${{ github.workspace }}":/github/workspace \
            -w /github/workspace \
            ghcr.io/arii/boomtick:latest \
            /workspace/scripts/impact-ai-code-review.ts github-models
        fi
