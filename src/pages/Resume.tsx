import React from 'react';
import ResumePage, { ResumePageProps } from './ResumePage';

const Resume: React.FC<ResumePageProps> = (props) => {
  return <ResumePage {...props} />;
};

export default Resume;
